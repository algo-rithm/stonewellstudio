<h1>Listing collection_groups</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @collection_groups.each do |collection_group| %>
      <tr>
        <td><%= collection_group.name %></td>
        <td><%= link_to 'Show', collection_group %></td>
        <td><%= link_to 'Edit', edit_collection_group_path(collection_group) %></td>
        <td><%= link_to 'Destroy', collection_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Collection group', new_collection_group_path %>
